#include <main.h>
#include <ctype.h>

/**
 * _islower - return 1 or 0
 * @c: The character to print;
 *
 * Return: On Success 1
 * Otherwise: 0 is returned
 */
int _islower(int c)
{
	if (int islower(int c))
		return (1);
	else
		return (0);

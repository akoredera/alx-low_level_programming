#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i, n;
	char c, str[26];
	for (i = 0; i <= 26; i++)
		str[i] = getchar();
	for (n = 0; n <= 26; i++)
		putchar(str[n]);

	return (0);
}

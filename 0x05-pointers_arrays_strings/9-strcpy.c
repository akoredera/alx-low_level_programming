#include "string.h"
/**
 * _strcpy - copy string from one string to other
 * @src: source to copy
 * @dest: location to copy into
 *
 * Return: dest variable value
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
